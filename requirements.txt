# Core ETL and Data Engineering
pyspark==3.5.1
requests==2.32.3
great-expectations==1.4.6

# Airflow (if running ETL locally, not in Docker)
apache-airflow==2.8.1
apache-airflow-providers-smtp==1.8.0

# FastAPI/Flask (if using API in Python)
fastapi==0.115.12
uvicorn==0.34.2
Flask==2.2.5

# Utilities
python-dotenv==1.1.0
PyYAML==6.0.2

# Data validation and schema
jsonschema==4.24.0

# Remove all other packages unless you have a specific use for them in your codebase.
